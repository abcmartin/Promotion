"""Multi-Agenten-Pipeline (Planner → Auditor → Executor → Verifier).

Dieses Paket stellt eine minimale, lauffähige Orchestrierung bereit,
die vorhandene Manuskriptdateien verarbeitet und Artefakte in
`plans/`, `audit/`, `drafts/`, `verify/` und `reports/` erzeugt.
"""

